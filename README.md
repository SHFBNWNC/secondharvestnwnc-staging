# Second Harvest Food Bank of NWNC — Homepage Prototype

Website redesign prototype for committee review.

## Deploy

Connected to Netlify via GitHub — pushes to `main` auto-deploy.

## Structure

```
index.html          ← Full homepage prototype
assets/images/      ← Extracted image assets
netlify.toml        ← Deploy + caching config
```
